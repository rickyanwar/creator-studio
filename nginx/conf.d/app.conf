# Development config (HTTP only)
# Production: replace with HTTPS + certbot SSL block

server {
    listen 80;
    server_name _;

    # ── Media CDN: serve images from shared volume ──
    location /media/ {
        alias /opt/studio/media/;
        autoindex off;

        add_header Cache-Control "public, max-age=604800";
        add_header X-Content-Type-Options nosniff;
        add_header Access-Control-Allow-Origin "*";

        location ~* \.(jpg|jpeg|png|webp|gif)$ {
            try_files $uri =404;
            expires 7d;
        }

        # Block directory traversal
        location ~ /\. { deny all; }
    }

    # ── API proxy ─────────────────────────────────────
    location /api/ {
        proxy_pass http://api:8000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 10s;
    }

    # ── Frontend ──────────────────────────────────────
    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# ── Production HTTPS template (uncomment on VPS) ─────
# server {
#     listen 443 ssl http2;
#     server_name cdn.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/cdn.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cdn.yourdomain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location /media/ {
#         alias /opt/studio/media/;
#         autoindex off;
#         add_header Cache-Control "public, max-age=604800";
#         add_header X-Content-Type-Options nosniff;
#         location ~* \.(jpg|jpeg|png|webp)$ { try_files $uri =404; expires 7d; }
#         location ~ /\. { deny all; }
#     }
# }
#
# server {
#     listen 80;
#     server_name cdn.yourdomain.com;
#     return 301 https://$host$request_uri;
# }
